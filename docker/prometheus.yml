global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # In a real environment, you'd add exporters here (e.g. php-fpm_exporter, postgres_exporter)
  # - job_name: 'netsight-app'
  #   static_configs:
  #     - targets: ['app:9253']
